# =========================================================
# SPATIAL KEY
# =========================================================

def spatial_key(x, y):

    return f"{x},{y}"


# =========================================================
# PARSE SPATIAL KEY
# =========================================================

def parse_spatial_key(key):

    x, y = key.split(",")

    return int(x), int(y)